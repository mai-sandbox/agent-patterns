# =============================================================================
# LangGraph Agent Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual API keys and configuration
# 
# IMPORTANT: Never commit your actual .env file to version control!
# The .env file should contain your real API keys and secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider API Keys
# -----------------------------------------------------------------------------
# Anthropic API Key (Preferred LLM Provider)
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (Alternative LLM Provider)
# Get your API key from: https://platform.openai.com/api-keys
# Uncomment the line below if you want to use OpenAI instead of Anthropic
# OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# Tool API Keys
# -----------------------------------------------------------------------------
# Tavily Search API Key (Required for web search functionality)
# Get your API key from: https://app.tavily.com/
TAVILY_API_KEY=your_tavily_api_key_here

# -----------------------------------------------------------------------------
# LangGraph Configuration
# -----------------------------------------------------------------------------
# LangGraph API URL (Local development server)
# Default: http://localhost:2024 (when running 'langgraph dev')
# Change this if you're running the server on a different host/port
LANGGRAPH_API_URL=http://localhost:2024

# -----------------------------------------------------------------------------
# LangSmith Configuration (Optional but Recommended)
# -----------------------------------------------------------------------------
# LangSmith API Key for tracing and monitoring
# Get your API key from: https://smith.langchain.com/settings
# LANGSMITH_API_KEY=your_langsmith_api_key_here

# LangSmith Project Name (Optional)
# LANGSMITH_PROJECT=langgraph-agent-feedback

# Enable/disable LangSmith tracing
# LANGSMITH_TRACING=true

# -----------------------------------------------------------------------------
# Development Configuration
# -----------------------------------------------------------------------------
# Set to 'development' for local development, 'production' for deployment
ENVIRONMENT=development

# Enable debug mode for additional logging
DEBUG=true

# -----------------------------------------------------------------------------
# Setup Instructions
# -----------------------------------------------------------------------------
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your actual API keys in the .env file
#
# 3. Install dependencies:
#    pip install -r requirements.txt
#
# 4. Start the LangGraph server:
#    langgraph dev
#
# 5. In a new terminal, start the Streamlit app:
#    streamlit run app.py
#
# 6. Open your browser to http://localhost:8501
#
# -----------------------------------------------------------------------------
# Troubleshooting
# -----------------------------------------------------------------------------
# If you encounter issues:
#
# 1. Verify all API keys are correctly set in your .env file
# 2. Ensure the LangGraph server is running on the correct port
# 3. Check that all dependencies are installed: pip list
# 4. Verify your Python version is >= 3.11
# 5. Check the console output for any error messages
#
# For more help, see the README.md file
# -----------------------------------------------------------------------------
