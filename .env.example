# LangGraph Chatbot Environment Configuration
# Copy this file to .env and fill in your actual API keys

# =============================================================================
# LLM Provider API Keys
# =============================================================================
# You only need to configure ONE of the following providers to use the chatbot.
# The chatbot will automatically detect which provider is available.

# OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
# Used for: GPT-4o-mini model
OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key  
# Get your API key from: https://console.anthropic.com/settings/keys
# Used for: Claude-3.5-Sonnet model
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Google API Key
# Get your API key from: https://ai.google.dev/gemini-api/docs/api-key
# Used for: Gemini-2.0-Flash model
GOOGLE_API_KEY=your-google-api-key-here

# =============================================================================
# Optional: LangSmith Configuration (for tracing and monitoring)
# =============================================================================
# LangSmith provides powerful debugging and monitoring capabilities for LangGraph applications.
# Sign up at: https://smith.langchain.com/

# LangSmith API Key
LANGSMITH_API_KEY=your-langsmith-api-key-here

# LangSmith Project Name (optional - defaults to "default")
LANGSMITH_PROJECT=chatbot-project

# Enable LangSmith tracing (optional - set to "true" to enable)
LANGCHAIN_TRACING_V2=false

# LangSmith endpoint (optional - uses default if not specified)
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com

# =============================================================================
# Configuration Notes
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in at least ONE LLM provider API key
# 3. Keep your .env file secure and never commit it to version control
# 4. The chatbot will automatically use the first available provider in this order:
#    - OpenAI (if OPENAI_API_KEY is set)
#    - Anthropic (if ANTHROPIC_API_KEY is set)  
#    - Google (if GOOGLE_API_KEY is set)
# 5. LangSmith configuration is optional but recommended for debugging
